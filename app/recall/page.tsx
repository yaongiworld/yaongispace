import { SectionPage } from "../SectionPage";

/**
 * 기억 — Recall.
 *
 * Empty by design, and like the map it is **not part of the skeleton** (see
 * ORCHESTRATION.md). The tab exists because Recall is one of the six sections
 * in scope for this iteration.
 */
export default function RecallPage() {
  return <SectionPage title="기억" />;
}
