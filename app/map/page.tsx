import { SectionPage } from "../SectionPage";

/**
 * 지도 — the world map of Places and Visits.
 *
 * Empty by design, and it will stay empty longer than its neighbours: the world
 * map is **not part of the skeleton at all** (see ORCHESTRATION.md, "What is not
 * in the skeleton"). The tab exists because the map is one of the six sections
 * in scope for this iteration; no ticket 05–08 fills it in.
 */
export default function MapPage() {
  return <SectionPage title="지도" />;
}
